services:
  - type: web
    name: navisense-ml
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-lite.txt
    startCommand: uvicorn app-lite:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
