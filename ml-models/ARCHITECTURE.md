# ML System Architecture v2.0

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     SSABIRoad ML System                      │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ Data         │    │ Training     │    │ Inference    │
│ Collection   │───▶│ Pipeline     │───▶│ API          │
└──────────────┘    └──────────────┘    └──────────────┘
        │                     │                     │
        │                     │                     ▼
        │                     │           ┌──────────────┐
        │                     │           │ Monitoring & │
        │                     └──────────▶│ Versioning   │
        │                                 └──────────────┘
        │                                         │
        └─────────────────────────────────────────┘
                    Active Learning Loop
```

## 📊 Component Architecture

### 1. Data Collection Layer

```
┌─────────────────────────────────────────┐
│        Data Collection Pipeline          │
├─────────────────────────────────────────┤
│                                          │
│  ┌──────────────┐  ┌─────────────────┐ │
│  │ OpenStreetMap│  │ Google Street   │ │
│  │ (Overpass)   │  │ View API        │ │
│  └──────┬───────┘  └────────┬────────┘ │
│         │                   │          │
│         └───────┬───────────┘          │
│                 ▼                      │
│         ┌──────────────┐               │
│         │ User Uploads │               │
│         └──────┬───────┘               │
│                │                       │
│                ▼                       │
│      ┌──────────────────┐              │
│      │ Data Validator   │              │
│      │ & Preprocessor   │              │
│      └──────────────────┘              │
└─────────────────────────────────────────┘
```

### 2. Training Pipeline

```
┌─────────────────────────────────────────────────────┐
│              Training Orchestrator                   │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐│
│  │ Geolocation  │  │  Landmark    │  │   FAISS   ││
│  │   Model      │  │ Classifier   │  │   Index   ││
│  │              │  │              │  │           ││
│  │ EfficientNet │  │ EfficientNet │  │   CLIP    ││
│  │ + Haversine  │  │ + Focal Loss │  │  ViT-B32  ││
│  └──────┬───────┘  └──────┬───────┘  └─────┬─────┘│
│         │                 │                 │      │
│         └─────────────────┼─────────────────┘      │
│                           ▼                        │
│                  ┌─────────────────┐               │
│                  │ Model Registry  │               │
│                  │  & Versioning   │               │
│                  └─────────────────┘               │
└─────────────────────────────────────────────────────┘
```

### 3. Inference Pipeline

```
┌─────────────────────────────────────────────────────┐
│              Fusion Pipeline (Inference)             │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Input Image                                         │
│       │                                              │
│       ▼                                              │
│  ┌─────────────────┐                                │
│  │ CLIP + FAISS    │  Similarity > 0.75?            │
│  │ Search          │────Yes───▶ Return Result       │
│  └────────┬────────┘                                │
│           │ No                                       │
│           ▼                                          │
│  ┌─────────────────┐                                │
│  │ OCR + Geocoding │  Location Found?               │
│  │ (EasyOCR)       │────Yes───▶ Return Result       │
│  └────────┬────────┘                                │
│           │ No                                       │
│           ▼                                          │
│  ┌─────────────────┐                                │
│  │ Landmark        │  Confidence > 0.6?             │
│  │ Detection       │────Yes───▶ Add Context         │
│  └────────┬────────┘                                │
│           │                                          │
│           ▼                                          │
│  ┌─────────────────┐                                │
│  │ Geolocation     │                                │
│  │ Model           │────────▶ Return Prediction     │
│  └─────────────────┘                                │
└─────────────────────────────────────────────────────┘
```

### 4. Active Learning System

```
┌─────────────────────────────────────────────────────┐
│            Active Learning Pipeline                  │
├─────────────────────────────────────────────────────┤
│                                                      │
│  User Predictions                                    │
│       │                                              │
│       ├──▶ High Confidence (>0.8) ──▶ Queue         │
│       │                                              │
│       └──▶ User Corrections ──▶ Priority Queue      │
│                                                      │
│  ┌──────────────────────────────────────┐           │
│  │        Training Queue                │           │
│  │  ┌────────────────────────────────┐  │           │
│  │  │ High Priority (Corrections)    │  │           │
│  │  │ Normal Priority (Confident)    │  │           │
│  │  └────────────────────────────────┘  │           │
│  └──────────────┬───────────────────────┘           │
│                 │                                    │
│                 ▼                                    │
│  Should Retrain? (100+ samples OR 20+ corrections)  │
│                 │                                    │
│                 ▼                                    │
│  ┌──────────────────────────────────────┐           │
│  │  Prepare Training Batch              │           │
│  │  Combine with Existing Data          │           │
│  │  Train New Model Version             │           │
│  │  Evaluate & Register                 │           │
│  └──────────────────────────────────────┘           │
└─────────────────────────────────────────────────────┘
```

### 5. Monitoring & Versioning

```
┌─────────────────────────────────────────────────────┐
│         Model Monitoring & Version Control           │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────┐           │
│  │      Prediction Logger               │           │
│  │  • Image ID                          │           │
│  │  • Prediction                        │           │
│  │  • Ground Truth (if available)       │           │
│  │  • User Feedback                     │           │
│  │  • Timestamp                         │           │
│  └──────────────┬───────────────────────┘           │
│                 │                                    │
│                 ▼                                    │
│  ┌──────────────────────────────────────┐           │
│  │      Metrics Calculator              │           │
│  │  • Mean/Median Error                 │           │
│  │  • Accuracy @1km/5km/25km            │           │
│  │  • User Satisfaction                 │           │
│  │  • Method Distribution               │           │
│  └──────────────┬───────────────────────┘           │
│                 │                                    │
│                 ▼                                    │
│  ┌──────────────────────────────────────┐           │
│  │      Model Version Manager           │           │
│  │  • Registry (all versions)           │           │
│  │  • Active Model                      │           │
│  │  • Best Model                        │           │
│  │  • Auto-Switch Logic                 │           │
│  └──────────────────────────────────────┘           │
└─────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Training Flow

```
1. Data Collection
   ├─▶ OSM Buildings → Download metadata
   ├─▶ Street View → Download images (4 angles)
   └─▶ User Uploads → Collect from API

2. Data Preparation
   ├─▶ Validate images and GPS
   ├─▶ Split train/val (80/20)
   └─▶ Apply augmentation

3. Model Training
   ├─▶ Geolocation Model (20 epochs)
   ├─▶ Landmark Classifier (30 epochs)
   └─▶ FAISS Index Build

4. Model Registration
   ├─▶ Save with version timestamp
   ├─▶ Record metrics
   └─▶ Set as active if best

5. Deployment
   └─▶ Load in inference API
```

### Inference Flow

```
1. Image Upload
   └─▶ Receive via API

2. Fusion Pipeline
   ├─▶ Try FAISS (fastest)
   ├─▶ Try OCR (if no match)
   ├─▶ Try Landmark (add context)
   └─▶ Use Geolocation (fallback)

3. Result Processing
   ├─▶ Log prediction
   ├─▶ Check confidence
   └─▶ Add to active learning queue

4. Return to User
   └─▶ JSON response with location + confidence
```

### Active Learning Flow

```
1. Collect Samples
   ├─▶ High confidence predictions
   └─▶ User corrections (priority)

2. Queue Management
   ├─▶ Check threshold (100 samples)
   └─▶ Prioritize corrections

3. Trigger Training
   ├─▶ Prepare batch
   ├─▶ Combine with existing data
   └─▶ Train new version

4. Evaluate & Deploy
   ├─▶ Compare with current
   ├─▶ Register new version
   └─▶ Auto-switch if better
```

## 🎯 Model Details

### Geolocation Model
- **Input**: 224x224 RGB image
- **Backbone**: EfficientNet-B0 (pretrained)
- **Head**: 2-layer MLP with dropout
- **Output**: (latitude, longitude)
- **Loss**: Haversine distance
- **Optimizer**: AdamW (lr=1e-4)
- **Scheduler**: CosineAnnealingLR

### Landmark Classifier
- **Input**: 224x224 RGB image
- **Backbone**: EfficientNet-B0 (pretrained)
- **Head**: 2-layer MLP with dropout
- **Output**: Class probabilities
- **Loss**: Focal Loss (γ=2)
- **Optimizer**: AdamW (lr=1e-4)
- **Scheduler**: CosineAnnealingLR

### CLIP + FAISS
- **Model**: OpenAI CLIP (ViT-B-32)
- **Embedding**: 512-dimensional
- **Index**: FAISS IndexFlatIP
- **Similarity**: Cosine similarity
- **Threshold**: 0.75

## 📈 Performance Characteristics

| Component | Latency | Accuracy | Memory |
|-----------|---------|----------|--------|
| CLIP Embedding | ~50ms | - | 500MB |
| FAISS Search | ~5ms | 95% @top-5 | 100MB |
| OCR | ~200ms | 80% | 1GB |
| Geolocation | ~60ms | <25km | 200MB |
| Landmark | ~50ms | 85% @top-5 | 200MB |
| **Full Pipeline** | **~300ms** | **90%** | **2GB** |

## 🔧 Scalability

### Horizontal Scaling
- Multiple API instances behind load balancer
- Shared FAISS index (read-only)
- Distributed training with PyTorch DDP

### Vertical Scaling
- GPU acceleration (CUDA)
- Batch inference
- Model quantization (INT8)
- ONNX export for faster inference

### Storage
- Models: ~200MB each
- FAISS Index: ~1MB per 1000 buildings
- Logs: ~1GB per month (high traffic)

## 🔒 Security & Privacy

- API authentication (JWT)
- Rate limiting per user
- PII removal from logs
- Secure model storage
- HTTPS in production
- Input validation

---

**Version**: 2.0.0  
**Last Updated**: 2025
