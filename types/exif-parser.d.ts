declare module 'exif-parser' {
    // You can define basic types for the module or leave it as `any` for now.
    // Example for a simple function that returns an object
    export function parse(buffer: Buffer): any;
  }
  